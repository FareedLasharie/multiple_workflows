name: cwt-release-stg

on:
  pull_request:
    types: [opened, reopened, synchronize]
       


jobs:
  docker:
    runs-on: ubuntu-latest

    steps:

      - name: workflow 1
        run: echo "workflow 1"     